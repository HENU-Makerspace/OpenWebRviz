{
  "name": "server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run dev:local",
    "dev:local": "ROBOT_CONFIG_PROFILE=local bun run --watch src/index.ts",
    "dev:cloud": "ROBOT_CONFIG_PROFILE=cloud bun run --watch src/index.ts",
    "start": "bun run start:local",
    "start:local": "ROBOT_CONFIG_PROFILE=local bun run src/index.ts",
    "start:cloud": "ROBOT_CONFIG_PROFILE=cloud bun run src/index.ts",
    "build": "tsc",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "drizzle-orm": "^0.38.0",
    "better-auth": "^1.2.0",
    "@hono/node-server": "^1.13.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.0",
    "typescript": "^5.7.0",
    "drizzle-kit": "^0.30.0",
    "@types/node": "^22.10.0"
  }
}
